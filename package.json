{
  "name": "jeff-brydon-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website with JeffBot RAG integration",
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "live-server --port=8080 --open=/index.html --watch=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}

